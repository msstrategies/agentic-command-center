# Copy this file to .env and fill in real values. Never commit .env.
# Every variable is optional. The eval harness and hooks run with zero keys.

# Model provider (used by the model cascade described in the self-healing rule)
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=

# Example tool credential referenced by the sarah-ai demo target's live mode
APIFY_API_KEY=
